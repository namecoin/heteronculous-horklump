### Starting Tasks:
  1. Write a simple Go program that launches an application with ptrace 
  2. Write a simple Go program that uses ptrace to detect when socket system calls are issued, and prints a notice when this happens.
